import { boot } from 'quasar/wrappers';

export default boot(async ({ app }) => {
	// app.directive('directive-example', {
	// 	created: (el, props) => {
	// 		el.addEventListener('click', function (e) {

	// 		});
	// 	},
	// });
});
